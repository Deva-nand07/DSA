void sortZeroesAndOne(int input[], int size)
{
    int i=0, j=size-1;
    while(i<j)
    {
        if(input[i]==0){
            i++;
        }
        else if((input[i]==1) && (input[j]==0)){
            swap(input[i], input[j]);
        }
        else{
            j--;
        }
    }
}
